<h1>All users</h1>

<% if @settings.blank? %>
    <p>There are no settings</p>
<% else %>
    <ul>
        <% @settings.each do |f| %>
            <li><%= f.id %></li>
        <% end %>
    </ul>
<% end %>
